<h3>Your Food Inventory</h3>
<ul class='itemsIndex'>
  <% current_user.items.each do |item| %>
  <li><%= item.id.to_s %> | <%= item.item_kind_id.to_s %> <%= item.item_kind.name %> expires at some point
      <span class="date"><%= item.expiration.to_s %></span>.
  </li>
  <% end %>
</ul>