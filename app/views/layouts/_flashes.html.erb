<!-- Look up how they handle flashes on  -->
<% if flash.notice || flash.alert %>
  <% flash.each do |key, msg| %>
    <% key = "success" if key = "notice" %>
    <%= content_tag :div, msg, :class => "alert alert-#{key}" %>
  <% end %>
<% end %>